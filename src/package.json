{
  "name": "requesttest",
  "version": "0.0.1",
  "description": "a simple test app how to make http get request in node",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Johannes Vitt",
  "license": "ISC",
  "dependencies": {
    "http": "0.0.0",
    "request": "^2.80.0"
  }
}
